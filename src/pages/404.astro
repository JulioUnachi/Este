---
const error = "4🌑​​4";

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content="pagina, 404, busqueda" />
    <meta name="generator" content={Astro.generator} />
    <title>404</title>
    <style>
      .contenedor {
        width: 100%;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        flex-direction: column;
        color: white;
      }
      h1 {
        font-weight: 600;
        font-size: 200px;
      }
      body {
        width: 100%;
        background-image: url(../images/404.jpg);
        background-size: cover; /* Para cubrir todo el fondo */
        background-position: center; /* Para centrar la imagen */
        background-repeat: no-repeat; /* Para evitar repeticiones */
        text-align: center;
      }

      h4 {
        padding-bottom: 40px;
      }

      a {
        border-radius: 20px; /* Ajusta el valor para redondear más o menos */
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
        background: linear-gradient(
          90deg,
          #e2b0ff,
          #9f44d3
        ); /* Color de fondo */
        color: white;
        border: none;
        outline: none; /* Sin contorno */
      }
      a:hover {
        background-color: pink;
      }

      /* Media query para teléfonos (mayoría de los smartphones) */
      @media only screen and (max-width: 767px) {
        /* Estilos específicos para teléfonos */
        h1 {
          font-weight: 600;
          font-size: 100px;
        }
      }
    </style>
  </head>
  <body>
    <!-- contendor del contenido principal -->
    <div class="contenedor">
      <h1>{error}</h1>

      <!-- contenido del 404 -->
      <div class="contenido">
        <span>ups</span>
        <h4>¡No es la pagina que Buscabas¡</h4>
        <a href="">Volver</a>
      </div>
    </div>
  </body>
</html>
